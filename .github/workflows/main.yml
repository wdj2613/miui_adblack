name: Schedule Worker
on:
  push:
    # 分支随意
    branches:
      - master
jobs:
  work:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: ls -lah
      - run: python3 -m pip install requests
      - run: python3 ./miui_adblack

